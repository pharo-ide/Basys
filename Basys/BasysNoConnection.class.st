"
I am an error about connectivity issue with given remote peer when no connection exists and nothing new can be established.
I am signaled by BasysPassiveConnectionPool which is not able to open new connection for the user and therefore it fails to allocate connection when nothing is pooled
"
Class {
	#name : #BasysNoConnection,
	#superclass : #Error,
	#category : #Basys
}
